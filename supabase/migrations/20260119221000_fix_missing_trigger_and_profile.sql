-- ============================================================================
-- 🔧 КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Триггер и профиль
-- ============================================================================
-- Дата: 2026-01-19 22:10
-- Проблема: Триггер on_auth_user_created не существует, профиль не создаётся
-- Решение: Создать триггер и профиль для существующего пользователя
-- ============================================================================

-- 1. Создать профиль для существующего пользователя (если его нет)
INSERT INTO public.profiles (id)
SELECT id FROM auth.users
WHERE id NOT IN (SELECT id FROM public.profiles)
ON CONFLICT (id) DO NOTHING;

-- 2. Создать триггер для автоматического создания профиля
-- ВАЖНО: Триггер должен быть создан через Supabase Dashboard в разделе Database > Triggers
-- Или через SQL Editor с правами superuser:
--
-- CREATE TRIGGER on_auth_user_created
--   AFTER INSERT ON auth.users
--   FOR EACH ROW
--   EXECUTE FUNCTION public.handle_new_user();
--
-- Если триггер не создаётся автоматически, создайте его вручную в Dashboard.
